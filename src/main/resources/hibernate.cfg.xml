<?xml version="1.0" encoding="UTF-8"?>


<hibernate-configuration>
    <session-factory>
        <!-- PostgreSQL JDBC Driver -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>

        <!-- Database Connection URL -->
        <property name="connection.url">jdbc:mysql://localhost:3306/myhiber</property>

        <!-- PostgreSQL Username and Password -->
        <property name="connection.username">root</property>
        <property name="connection.password">Vaibhav143@</property>

        <!-- Hibernate Dialect for PostgreSQL -->
        <property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>

        <!-- Show SQL Queries in Console -->
        <property name="show_sql">true</property>

        <!-- Automatically Update Schema -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class = "com.tut.Student"/>
        <mapping class = "com.tut.Address"/>

        <mapping class = "com.onetomany.QuestionOneToMany"/>
        <mapping class = "com.onetomany.AnswerOneToMany"/>
    </session-factory>
</hibernate-configuration>